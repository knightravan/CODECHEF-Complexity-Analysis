import math
for i in range(int(input())):
    for j in range(int(input())):
        lst=list(map(int,input().split()))
        a=math.floor(lst[1]/2)
        b=math.ceil(lst[1]/2)
        if lst[0]==lst[2]:
            print(a)
        else:
            print(b)
